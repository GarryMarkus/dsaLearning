#include <bits/stdc++.h>
using namespace std;

int binary_search(int array[], int length, int key){
    
}

int main(){
    int n;
    cin >> n;

    int arr[n];

    for(int i=0; i<n; i++){
        cin >> arr[i];
    }

    int find;
    cin >> find;

    cout << binary_search(arr, n, find);

}