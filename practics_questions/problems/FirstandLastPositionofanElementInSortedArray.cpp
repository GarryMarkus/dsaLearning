#include<bits/stdc++.h>
using namespace std;

pair<int, int> firstAndLastPosition(vector<int>& arr, int n, int k)
{
    
}

int main(){
    int n, k;
    cin >> n;

    vector<int> arr;

    for (int i=0; i<n; i++){

    }
    firstAndLastPosition(arr, n, k);
}